apiVersion: v1
kind: Service
metadata:
  name: {{ include "npm-app-chart.fullname" . }}
  labels:
    {{- include "npm-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "npm-app-chart.selectorLabels" . | nindent 4 }}
